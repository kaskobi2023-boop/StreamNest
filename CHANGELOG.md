# Changelog

## 0.6.6

- 실제 치지직 장시간 HLS 시험에서 확인된 길이 판정 오탐 수정. 영상 메타데이터를 컨테이너의 음성 정렬/패딩이 포함된 길이 대신 영상 트랙 길이와 비교하고, 트랙 길이가 없을 때만 컨테이너 값을 사용.
- 기존 허용 오차는 늘리지 않음. 음성이 길어 컨테이너 전체 길이는 맞지만 영상 트랙이 짧은 경우도 검출하도록 회귀 시험 추가.

## 0.6.5

- 치지직·YouTube에도 누락 조각 중단, 미디어 정보·전체 디코딩 검증 및 검증 전 임시 저장을 적용.
- 분석한 길이와 음성 유무를 모든 화질 선택지에 보존하여 완료 검사에 사용.
- 기존 저화질 파일을 높은 화질 요청의 결과로 재사용하지 않도록 최종 폴더와 엔진 작업 폴더 분리. 화질별 파일명과 중복 번호로 기존 파일 보호.
- 공통 음성 URL만으로 서로 다른 HLS 영상을 합치지 않도록 동일성 조건 보완. 동일 비디오의 중복 제거는 유지.
- 일반 웹 주소 갱신 후 목록 순번이 바뀌어도 최초 작업의 임시 폴더를 유지하여 취소 후 정리 누락 방지.
- 엔진 실패 시 이어받기 체크포인트도 해당 작업 안에 격리하고 재시도는 새 시도 폴더에서 시작. 조각 누락 이후의 잘못된 이어받기 성공 방지.
- 치지직 range-MPD 보완 파서에서 range 없는 sourceURL 초기화 조각을 보존. 전체 파일 초기화의 크기는 미상으로 처리.
- 위 실패 조건의 단위·플러그인·실제 엔진 회귀 시험 추가.
- GitHub용 소스 ZIP, 의존성 준비 스크립트, 루트 빌드 안내와 민감/생성 파일 제외 규칙 추가.

## 0.6.4

- 일반 웹 브라우저 호환 전송을 페이지뿐 아니라 재생목록·하위 목록·조각에도 적용.
- HLS 접근 거부와 주소 미지원 오류를 구분.
